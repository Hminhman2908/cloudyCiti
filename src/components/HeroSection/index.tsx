import { Button } from "@mui/material";

const HeroSection = () => {
    return (
        <div>
            <Button variant="contained">Contained</Button>
        </div>
    )
}
export default HeroSection;